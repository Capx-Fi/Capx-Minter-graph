{
  "name": "mint-ethereum",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ varun-capx/mint-ethereum",
    "create-local": "graph create --node http://localhost:8020/ varun-capx/mint-ethereum",
    "remove-local": "graph remove --node http://localhost:8020/ varun-capx/mint-ethereum",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 varun-capx/mint-ethereum"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
